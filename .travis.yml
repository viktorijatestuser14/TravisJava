os: linux

language: java

cache: bundler

dist: bionic

install: skip

jobs:
  include:
    - name: "Java 8 Build"
      jdk: openjdk8
      script:
        - echo "Running build with Java 8"
        - true # Replace with your actual build and test commands for Java 8

    - name: "Java 11 Build"
      jdk: openjdk11
      script:
        - echo "Running build with Java 11"
        - true # Replace with your actual build and test commands
        



  
  
